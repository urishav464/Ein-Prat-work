# Mishmar management app — שנה ב' תשפ"ז (5787 / 2026-27)
streamlit>=1.40
pyluach>=2.2          # Hebrew/Gregorian dates

# Speaker discovery. The package was renamed from `duckduckgo-search` to `ddgs`;
# install `ddgs` and import `from ddgs import DDGS`. It scrapes DuckDuckGo's
# endpoints, so expect RatelimitException under load — speaker_search.py must
# back off and fall back to showing the query rather than failing the page.
ddgs>=9.0

# sqlite3 is in the Python standard library — nothing to install.
# The chat layer. Note the SDK went 0.x -> 1.x; this code targets 1.x
# (httpx2-based, and `budget_tokens` is rejected on current models).
anthropic>=1.0

# Required by Streamlit's native OIDC login (st.login / st.user), which is how
# the 10 trainees sign in once the app is deployed. Not needed to run locally.
#
# Install into a venv. This pulls `cryptography`, and against a Debian/Ubuntu
# system Python pip cannot replace the distro-managed copy — it fails with
# "Cannot uninstall cryptography, RECORD file not found" and aborts the whole
# install. Escape hatch without a venv:
#     pip install --ignore-installed cryptography Authlib
Authlib>=1.3.2
