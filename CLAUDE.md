# Ein Prat Work — הנחיות עבודה

ארגז הכלים החינוכי של עין פרת. המטרה ארוכת הטווח: שכל הכלים שנבנו כאן — מחולל המשמרים, מחוללי שיעורים, מחוללי מסלולים ועוד — יהיו נגישים לחניכים ולצוות דרך צ'אטבוט אחד.

## מוסכמות

- **שפה:** שמות קבצים ותיקיות באנגלית, תוכן בעברית.
- **תאריכים:** תמיד עברי + לועזי יחד (למשל: `כ״א אלול תשפ״ו | 3.9.2026`).
- **לא ממציאים תוכן.** נושאים, מרצים, טקסטים ותאריכים מגיעים מהמשתמש. שדות לא ידועים נשארים `TBD` ולא מתמלאים בניחוש.
- **אי-התאמות מסמנים, לא מתקנים בשקט.** אם משהו בחומרי המקור סותר את עצמו — לרשום הערה ולשאול, לא לתקן על דעת עצמנו.

## תמונות ורקעים להזמנות

**אין לי יכולת לייצר תמונות.** לכן זרימת העבודה היא:

1. המשתמש אומר מה נושא המשמר.
2. **אני כותב פרומפט** מותאם לנושא — מבוסס על `Invitations/prompt/base-prompt.md` ועל הווריאנטים ב-`Invitations/prompt/variants/`.
3. **המשתמש מייצר את התמונה בעצמו** ומעלה אותה לריפו.
4. אני מרכיב ממנה את ההזמנה.

**חריג:** אם המשתמש מעדיף להשתמש ברקע שכבר קיים ב-`Invitations/examples/` — משתמשים בו ומדלגים על שלבים 2–3.

אל תציע עיבוד פילטרים כתחליף לציור אמיתי. עיבוד תמונה (חידוד, הגדלה, טקסטורה) הוא לשיפור קובץ קיים בלבד, לא ליצירת סגנון אקוורל.

## העברת קבצים

תמונות שמודבקות בצ'אט לא נשמרות לדיסק ואי אפשר לכתוב אותן לריפו. כל קובץ שצריך להיכנס לריפו — המשתמש מעלה דרך GitHub או `git push`.

## מבנה

| תיקייה | מה יש בה |
|---|---|
| `Mishmer-section/` | הכל על משמרים: המחולל, תבניות, וכל שנה בתיקייה משלה |
| `Invitations/` | ספריית העיצוב להזמנות: פרומפטים, דוגמאות, סגנון הבית |

## גיט

הפיתוח על ענף `claude/mishmer-generator-setup-h5gxqx`. הענף `main` קיים כבסיס ל-Pull Requests בלבד.
