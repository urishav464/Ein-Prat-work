---
paths:
  - app.py
  - chat_panel.py
---

# Streamlit DOM context — generated, do not edit by hand

Generated by `scripts/streamlit_dom_context.py` from the installed bundle of **Streamlit 1.64.0**. A `data-testid` used in `RTL_CSS` that is not in this list is a dead rule. Regenerate after every Streamlit upgrade; `design-review` and `app-reviewer` read this instead of remembering.

## `data-testid` values present in the bundle (278)

`stAlert` · `stAlertContainer` · `stAlertContent` · `stAlertTitle` · `stApp` · `stAppDeployButton` · `stAppIframeResizerAnchor` · `stAppScrollToBottomContainer` · `stAppSkeleton` · `stAppViewContainer` · `stAudio` · `stAudioInput` · `stAudioInputActionButton` · `stAudioInputWaveSurfer` · `stAudioInputWaveformTimeCode` · `stBalloons` · `stBaseLinkButton` · `stBidiComponentIsolated` · `stBidiComponentRegular` · `stBottom` · `stBottomBlockContainer` · `stButton` · `stButtonGroup` · `stCameraInput` · `stCameraInputButton` · `stCameraInputSwitchButton` · `stCameraInputWebcamComponent` · `stCameraInputWebcamStyledBox` · `stChatInput` · `stChatInputApproveButton` · `stChatInputCancelButton` · `stChatInputFileUploadButton` · `stChatInputMicButton` · `stChatInputStopButton` · `stChatInputSubmitButton` · `stChatInputTextArea` · `stChatMessage` · `stChatMessageAvatarAssistant` · `stChatMessageAvatarCustom` · `stChatMessageAvatarUser` · `stChatMessageContent` · `stCheckbox` · `stClearCacheDialog` · `stCode` · `stCodeUnhighlighted` · `stColorPicker` · `stColorPickerBlock` · `stColorPickerPopover` · `stColumn` · `stConnectionStatus` · `stCountdown` · `stCustomComponentV1` · `stDataFrame` · `stDataFrameButtonActionMenu` · `stDataFrameButtonActionMenuTarget` · `stDataFrameColumnFormattingMenu` · `stDataFrameColumnMenu` · `stDataFrameColumnMenuTarget` · `stDataFrameColumnVisibilityMenu` · `stDataFrameGlideDataEditor` · `stDataFrameResizable` · `stDataFrameStatisticsChart` · `stDataFrameStatisticsContent` · `stDataFrameStatisticsMenu` · `stDataFrameStatisticsMetrics` · `stDataFrameTooltipContent` · `stDataFrameTooltipTarget` · `stDateInput` · `stDateInputCalendar` · `stDateInputClearButton` · `stDateInputError` · `stDateInputField` · `stDateInputHeaderPickerPopover` · `stDateInputQuickSelect` · `stDateInputQuickSelectPopover` · `stDateTimeInput` · `stDateTimeInputCalendar` · `stDateTimeInputClearButton` · `stDateTimeInputError` · `stDateTimeInputField` · `stDateTimeInputPopoverTime` · `stDeckGlJsonChart` · `stDeckGlJsonChartZoomButton` · `stDeployDialogCommunityCloudIcon` · `stDeployDialogCustomDeploymentIcon` · `stDeployDialogSnowflakeDeploymentIcon` · `stDialog` · `stDialogIcon` · `stDownloadButton` · `stDownloadButtonError` · `stEChartsChart` · `stEChartsChartError` · `stElementContainer` · `stElementToolbar` · `stElementToolbarButton` · `stElementToolbarButtonContainer` · `stEmpty` · `stErrorCodeBlock` · `stErrorElementStack` · `stEvent` · `stException` · `stExceptionMessage` · `stExceptionTraceRow` · `stExpandSidebarButton` · `stExpander` · `stExpanderDetails` · `stExpanderStepChevron` · `stExpanderStepConnector` · `stExpanderStepIcon` · `stFeedback` · `stFileChip` · `stFileChipDeleteBtn` · `stFileChipImagePreview` · `stFileChipName` · `stFileChips` · `stFileUploader` · `stFileUploaderDropzone` · `stFileUploaderDropzoneInput` · `stFileUploaderDropzoneInstructions` · `stForm` · `stFormSubmitButton` · `stFullScreenFrame` · `stGraphVizChart` · `stHeader` · `stHeaderActionElements` · `stHeading` · `stHeadingDivider` · `stHeadingIconWrapper` · `stHeadingWithActionElements` · `stHelp` · `stHelpDoc` · `stHelpMember` · `stHelpMemberDocName` · `stHelpMemberDocString` · `stHelpMemberDocType` · `stHelpMemberDocValue` · `stHelpMembersTable` · `stHelpName` · `stHelpType` · `stHelpValue` · `stHtml` · `stIFrame` · `stImage` · `stImageCaption` · `stImageContainer` · `stImageLink` · `stJson` · `stJsonColumnViewer` · `stJsonPathTooltip` · `stJsonPathTooltipBody` · `stLayoutWrapper` · `stLinkButton` · `stLogoLink` · `stLogoSpacer` · `stMain` · `stMainBlockContainer` · `stMainMenu` · `stMainMenuButton` · `stMainMenuDivider` · `stMainMenuItem` · `stMainMenuItemLabel` · `stMainMenuList` · `stMainMenuPopover` · `stMainMenuRecordingIndicator` · `stMarkdown` · `stMarkdownColumnEditor` · `stMarkdownColumnViewer` · `stMarkdownContainer` · `stMarkdownPre` · `stMenuButton` · `stMenuButtonBody` · `stMenuButtonButton` · `stMermaidChart` · `stMermaidError` · `stMetric` · `stMetricChart` · `stMetricDelta` · `stMetricDeltaDescription` · `stMetricLabel` · `stMetricValue` · `stMultiSelect` · `stMultiSelectDropdown` · `stMultiSelectTagsContainer` · `stNavSectionHeader` · `stNumberInput` · `stNumberInputClearButton` · `stNumberInputContainer` · `stNumberInputField` · `stNumberInputIcon` · `stNumberInputStepDown` · `stNumberInputStepUp` · `stPageLink` · `stPagination` · `stPaginationEllipsis` · `stPaginationNext` · `stPaginationPrev` · `stPlotlyChart` · `stPopover` · `stPopoverBody` · `stPopoverButton` · `stProgress` · `stProgressBarTrack` · `stRadio` · `stRadioCaption` · `stRadioCaptionSpacer` · `stRadioGroup` · `stRadioOption` · `stScreencast` · `stScreencastAudioCheckbox` · `stScreencastInstruction` · `stSelectbox` · `stSelectboxVirtualDropdown` · `stSidebar` · `stSidebarCollapseButton` · `stSidebarContent` · `stSidebarHeader` · `stSidebarNav` · `stSidebarNavItems` · `stSidebarNavSeparator` · `stSidebarNavViewButton` · `stSidebarResizeHandle` · `stSidebarUserContent` · `stSkeleton` · `stSkeletonElement` · `stSkillsInstallCallout` · `stSkillsNudge` · `stSkillsNudgeAnchor` · `stSlider` · `stSliderThumbValue` · `stSliderTickBar` · `stSnow` · `stSpace` · `stSpinner` · `stStatusWidget` · `stStatusWidgetNewYearsIcon` · `stStatusWidgetRunningIcon` · `stStatusWidgetRunningManIcon` · `stTab` · `stTabPanel` · `stTable` · `stTableStyledEmptyTableCell` · `stTableStyledTable` · `stTabs` · `stTabsScrollLeft` · `stTabsScrollRight` · `stText` · `stTextArea` · `stTextAreaRootElement` · `stTextInput` · `stTextInputClearButton` · `stTextInputErrorIcon` · `stTextInputField` · `stTextInputIcon` · `stTextInputRootElement` · `stThemeSwitcher` · `stTimeInput` · `stTimeInputClearButton` · `stTimeInputError` · `stTimeInputTimeDisplay` · `stToast` · `stToastContainer` · `stToastText` · `stToastViewButton` · `stToolbar` · `stToolbarActionButton` · `stToolbarActionButtonIcon` · `stToolbarActionButtonLabel` · `stToolbarActions` · `stTooltipIcon` · `stTopNavPopover` · `stTopNavPopoverBody` · `stTopNavSection` · `stUnsupportedBrowserDialog` · `stVegaLiteChart` · `stVideo` · `stVideoRecordedDialog` · `stVideoSubtitle` · `stWidgetLabel`

## Structural facts (measured in headless Chromium, Streamlit 1.64.0)

- `st.container(border=True)`: the border, radius and padding sit on the **`stVerticalBlock`
  itself** (emotion hash `xaibe2` in 1.62.0; unchanged through 1.64.0); it also carries `data-test-scroll-behavior`, but
  so do width-only, keyed and fragment wrappers (hash `1n6tfoc`) — that attribute is NOT a card
  selector. The app styles cards opt-in through their key (`.st-key-card-…`). The old `…BlockBorderWrapper` testid no longer exists.
- **`st.dialog`, popovers, toasts and the select dropdown are portaled to `<body>`** — OUTSIDE
  `stAppViewContainer`, so nothing scoped to that container reaches them (measured: a dialog's
  `parentElement` is `BODY` and its computed direction was `ltr` under an RTL app). The app names
  `stDialog` / `stPopoverBody` / `stToastContainer` / `stSelectboxVirtualDropdown` explicitly.
- **The selectbox is react-aria, not BaseWeb**: `[data-baseweb="select"]` matches zero nodes; the
  control is `[data-testid="stSelectbox"] div[role="group"]`, the value lives in its `input`.
- **`stMarkdownContainer` carries `margin-bottom: -16px`** (it cancels a trailing `<p>`'s margin).
  A raw-HTML block ending in a `<div>` loses 16px of real height and the next element draws over it.
- `st.container(width=N)` / `st.container(horizontal=True)`: the sizing is on a
  **`stLayoutWrapper`** around the block (`flex: 0 0 fit-content` for `width="content"`).
- `st.columns` → `stLayoutWrapper > stHorizontalBlock > stColumn`.
- `st.caption` → `stCaptionContainer` (NOT `stMarkdownContainer`); `st.feedback` →
  `stFeedback` / `stFeedbackButton`; `st.segmented_control` and `st.pills` → `stButtonGroup`
  (labels via DynamicButtonLabel, not markdown — need their own RTL rule).
- **`stButtonGroup` is the whole widget and is `display: block`** — the label plus ONE child
  `div` that is the actual button row (`flex`, `nowrap`, `overflow: auto hidden`, no gap). Layout
  rules belong on that child; `justify-content` on the group is a no-op. Its buttons carry NO
  testid of their own — the bundle builds theirs as `stBaseButton-${kind}`
  (`stBaseButton-segmented_control` / `-segmented_controlActive`), a template string, so they
  never appear in the list above: another blind spot for a testid-only audit, like `data-baseweb`
  and `role`. Streamlit rounds the row's END corners by DOM order and gives every button but the
  last `margin-inline-start: -1px` to collapse adjacent borders — both are physical-direction
  assumptions that land backwards under RTL.
- Material icons are ligature text: a `<span translate="no">` whose text is the icon NAME. The
  bundle sets `translate: no` in exactly two places, both icon components, and only some carry
  `stIconMaterial` — the status widget's tick is `stExpanderIconCheck`. A font override that
  reaches them paints the literal word (measured on 1.64: «check» clipped to «chec» beside
  «הסריקה הסתיימה»). Carve out `[data-testid="stIconMaterial"], [data-testid^="stExpanderIcon"],
  span[translate="no"]`.
- **The radio's DOM moved in 1.64** (measured against the 1.63 and 1.64 bundles): 1.63 is
  `div[stRadioGroup][role=radiogroup] > label[stRadioOption] > span(input) + div > div(row) >
  div(circle)`; 1.64 inserts a react-aria wrapper — `div[stRadioGroup] > div > label[stRadioOption]
  > span(input) + div(row) > div(circle)` — so every `radiogroup > label` rule went dead, and the
  circle sits one level higher. Anchors that hold in both: `label[data-testid="stRadioOption"]`,
  `[data-testid="stRadioGroup"] > *`, react-aria's `data-selected` / `data-focus-visible` on the
  label, the circle's emotion target class per version (`eqiohyi4` 1.62/63, `e1mpz0hj4` 1.64) and
  the structural `div:has(> div:only-child:empty)`. A role selector (`div[role=…]`) is a blind spot
  for a testid-only audit — the same class of miss as `data-baseweb`.
- The sidebar collapse is a `transform: translateX(-width)` in the bundle — wrong under RTL;
  `RTL_CSS` neutralises it.
- Keyed containers → class `st-key-<key>` on the `stVerticalBlock` / `stHorizontalBlock` itself.
